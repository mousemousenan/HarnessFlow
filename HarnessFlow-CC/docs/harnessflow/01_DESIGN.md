# Design

Status: Draft

由 `/hf-design` 产出。`Status` 改为 `Approved` 是人的动作。

## 现状证据

现有实现的关键位置，用真实路径行号，不凭印象：

- `<path:line>` — <这里现在做什么>

## 模块划分

| 模块 | 职责 | 不负责 |
| --- | --- | --- |
| <名称> | <单一职责> | <明确排除的职责> |

## 对外契约

<接口签名、请求响应结构、状态码或错误码语义。变更是否向后兼容>

## 内部接口

<模块之间的调用契约与数据结构>

## 错误语义

| 情况 | 行为 | 调用方如何区分 |
| --- | --- | --- |
| <失败场景> | <抛出/返回什么> | <可编程判别方式> |

## 状态与数据流

<状态机或数据流向。并发/事务边界在哪，谁持有真相>

## 关键不变量

后续实现不允许破坏的性质：

1. <不变量>

## 安全边界

- 认证授权：<谁能调用，怎么校验>
- 输入校验：<在哪一层，校验什么>
- 密钥与凭据：<从哪读，是否进日志>
- 网络暴露面：<新增哪些可达端点。没有认证的要在这里明确说明>

## 迁移与回滚

- 数据迁移：<需要吗，怎么做，能否幂等重跑>
- 回滚条件：<出问题时怎么退回，退回后数据是否一致>

## 验证策略

<怎么证明设计成立：单元/集成/端到端各覆盖什么，哪些性质需要专门的测试>

## 取舍

| 决策 | 选择 | 放弃的方案 | 理由 |
| --- | --- | --- | --- |
| <决策点> | <选了什么> | <没选什么> | <为什么> |

## 需求追溯

| 需求 ID | 设计组件 |
| --- | --- |
| R-001 | <模块 / 接口> |

每条需求至少对应一个组件。有需求无组件说明设计有空白。
