# Requirements

Status: Draft

由 `/hf-design` 产出并维护。`Status` 改为 `Approved` 是人的动作，Agent 不得代改。

## 背景

<为什么现在做这件事，当前的实际问题是什么>

## 目标

- <要达成的结果>

## 非目标

- <明确不做的事。这一节比目标更能防止范围蔓延>

## 功能需求

| ID | 需求 | 可验证结果 |
| --- | --- | --- |
| R-001 | <系统必须表现为什么> | <怎么观察到它成立> |

## 异常与边界行为

| ID | 场景 | 期望行为 |
| --- | --- | --- |
| R-101 | <输入缺失 / 依赖不可用 / 并发冲突 / 越界数值> | <明确的行为，含错误语义> |

## 质量约束

| ID | 维度 | 约束 |
| --- | --- | --- |
| R-201 | 性能 | <可测量的指标与测量方式> |
| R-202 | 安全 | <认证授权、输入校验、密钥处理、暴露面> |
| R-203 | 兼容性 | <向后兼容范围、数据迁移要求> |
| R-204 | 可运维 | <日志、监控、故障排查需要什么> |

## 验收场景

| ID | 场景 | 通过条件 |
| --- | --- | --- |
| A-001 | <端到端场景描述> | <可判定的通过条件> |

## 未决问题

| ID | 问题 | 影响面 | 状态 |
| --- | --- | --- | --- |
| Q-001 | <需要人决策的问题> | <不同答案会改变什么> | Open / Decided: <决定> |

`Status` 改为 `Approved` 前，所有会实质改变方案的 `Q-*` 必须为 `Decided`。
