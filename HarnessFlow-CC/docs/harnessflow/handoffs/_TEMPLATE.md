# Handoff: <任务 ID>

Status: Pending Review

这一行的 `Status` 只有人能改为 `Accepted`。任何 Agent 写 `Accepted` 都是违规，
`/hf-run` 会拒绝这样的交接文件。

## 任务

<任务目标与为什么它需要人工确认：产品判断 / 安全复核 / 迁移确认 / 真实环境证据>

## 已完成的实现

| 文件 | 改了什么 |
| --- | --- |
| `<path>` | — |

## 已通过的自动校验

| 命令 | 退出码 | 结论 |
| --- | --- | --- |
| `<command>` | 0 | — |

## 需要人做的检查

1. <具体动作> — 预期看到：<可判定的结果>
2. <具体动作> — 预期看到：<可判定的结果>

不要在这里写密钥、令牌或任何凭据。需要凭据的步骤只写"使用哪个凭据"，不写值。

## 不可逆动作提示

<涉及真实资金、生产环境、数据删除、外部不可逆操作的，在这里明确列出。没有则写「无」>

## 人工审核结论

<审核人填写。通过后把顶部 Status 改为 Accepted，不要改本文件之外的内容，然后重跑 /hf-run>
